-r requirements.txt

# Solana / Anchor Python integration
# Keep this environment focused on the policy-controller client path.
# x402 SVM work currently needs a separate environment because it resolves
# against newer solders / construct-typing releases.
anchorpy==0.21.0
solana>=0.36.6,<0.37
solders>=0.26,<0.27
construct-typing>=0.5.6,<0.6
